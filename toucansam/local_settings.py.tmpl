DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2', # Add 'postgresql_psycopg2', 'postgresql', 'mysql', 'sqlite3' or 'oracle'.
        'NAME': 'gmkit',                      # Or path to database file if using sqlite3.
        'USER': 'jeff',                      # Not used with sqlite3.
        'PASSWORD': '',                  # Not used with sqlite3.
        'HOST': '',                      # Set to empty string for localhost. Not used with sqlite3.
        'PORT': '',                      # Set to empty string for default. Not used with sqlite3.
    }
}
CHANNEL_PREFIX = "REPLACEME"

EMAIL_HOST='smtp.gmail.com'
EMAIL_HOST_USER = 'test@lofiart.com'
EMAIL_HOST_PASSWORD='' # PASSWORD GOES HERE
EMAIL_USE_TLS=True
DEBUG = TEMPLATE_DEBUG = True

CACHES = {
    'default': {
        'BACKEND': 'django.core.cache.backends.memcached.MemcachedCache',
        'LOCATION': '127.0.0.1:11211',
    }
}

ADDITIONAL_MIDDLEWARE_CLASSES = [
    'core.middleware.ExtraRunserverPrinting',
]
